# AGENTS.md

## Dev instructions
- Use python virtual environment available at project root to execute code
- Create a python virtual environment if one does not already exist
- Use uv to create virtual environment and manage the project
- Always run linting and code-formatting after every code change